datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model specialists {
  id             Int    @id @default(autoincrement())
  name           String
  specialization String
  phone          String
}

model services {
  id   Int    @id @default(autoincrement())
  name String
}
